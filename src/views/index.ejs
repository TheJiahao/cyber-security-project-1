<!doctype html>
<html lang="en">
    <body>
        <nav>
            <% if (user) { %>
                <a href=<%= `/user/${user.username}` %>><%= user.username %></a>
            <% } else { %>
                <a href="/login">Login</a>
                <a href="/register">Register</a>
            <% } %>
        </nav>
        Hello World!
    </body>
</html>
